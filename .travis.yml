language: node_js

node_js:
  - 10.14.0

# services:
#   - mongodb

cache:
  directories:
  - node_modules

git:
  depth: 3

script:
  - cp .env.example .env
  - jest --coverage --coverageReporters=text-lcov | coveralls && rm -rf ./coverage